


<Router AppAssembly="@typeof(Program).Assembly">
    <Found Context="routeData">
        <RouteView RouteData="@routeData" DefaultLayout="@typeof(Layout.MasterLayout)" />
        <FocusOnNavigate RouteData="@routeData" Selector="h1" />
    </Found>
    
    <NotFound>
        <LayoutView Layout="@typeof(Layout.MasterLayout)">
            <Pulse.Web.Components.Error.NotFound />   <!-- we'll create this -->
        </LayoutView>
    </NotFound>
    
</Router>






@* @using Microsoft.AspNetCore.Components.Authorization
@inject NavigationManager Navigation
@inject AuthService AuthService
@inject CustomAuthenticationStateProvider AuthProvider
@inject ILogger<Routes> Logger *@

@* <CascadingAuthenticationState>
    <Router AppAssembly="typeof(Program).Assembly">
        <Found Context="routeData">
            <AuthorizeRouteView RouteData="routeData" DefaultLayout="typeof(Layout.NoLayout)" >
                <NotAuthorized>
                    <p>You are not authorized. Redirecting to login...</p>
                    @{
                        Navigation.NavigateTo("/home", true);
                    }
                </NotAuthorized>
            </AuthorizeRouteView>
            <FocusOnNavigate RouteData="routeData" Selector="h1" />
        </Found>
        <NotFound>
            <LayoutView Layout="@typeof(Layout.NoLayout)">
                <p>Sorry, there's nothing at this address.</p>
            </LayoutView>
        </NotFound>
    </Router>
</CascadingAuthenticationState> *@

@* @code {
    private bool _tokenLoaded = false;

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender && !_tokenLoaded)
        {
            try
            {
                // Load token from localStorage (safe here in OnAfterRenderAsync)
                await AuthService.InitializeAsync();
                AuthProvider.NotifyStateChanged();
                _tokenLoaded = true;
                
                Logger.LogInformation("Token loaded and auth state updated");
            }
            catch (Exception ex)
            {
                Logger.LogError(ex, "Failed to initialize auth");
            }
        }
    }
}

 *@