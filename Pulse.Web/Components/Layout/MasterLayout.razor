@using Microsoft.FluentUI.AspNetCore.Components
@using Pulse.Models.Users
@using Pulse.Web.Services
@inject IJSRuntime JS
@inject AppState AppState
@inject DataTransferService dataTrf
@inherits LayoutComponentBase
@rendermode InteractiveServer

<style>
    .stack-vertical {
        row-gap: 0px !important;
    }

    :root {
        --datagrid-hover-color: var(--pulse-dark-colour);
    }

    .fluent-data-grid {
        --fluent-data-grid-resize-handle-color: var(--neutral-stroke-rest) !important;
    }
</style>

@* <BootstrapBlazorRoot> *@
@* <FluentLayout Style="height:100vh; display: flex;flex-direction:column; flex: 1 1; overflow: hidden;"> *@
<FluentLayout Style="height:100vh; overflow: hidden"> @*  ; display: flex;flex-direction:column; overflow: auto;"> *@
    @* <FluentStack Orientation="Orientation.Vertical" Width="100%" > *@
        <MasterAIMenu />
        <CascadingValue Value="this">
            @Body
        </CascadingValue>
    @* </FluentStack> *@
</FluentLayout>
@* </BootstrapBlazorRoot> *@


@code {

    User user;
    string? pTheme;

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            // user = await dataTrf.GetgvUser();
            // pTheme = user.UserSettings!.UserTheme ?? "pulse";
            // await JS.InvokeVoidAsync("applyPulseTheme", pTheme);
            // this.StateHasChanged();
        }
    }
    protected override void OnInitialized()
    {
        // Subscribe to state changes
        
        // AppState.OnThemeChange += async () =>
        // {
        //     await InvokeAsync(StateHasChanged);
        // };
    }

}
   